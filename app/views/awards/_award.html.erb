<div class="award">
  <%= link_to award.body, award_path(award) %>
  <div class="award__actions">
    <%= button_to "Delete",
                  award_path(award),
                  method: :delete,
                  class: "btn btn--light" %>
    <%= link_to "Edit",
                edit_award_path(award),
                class: "btn btn--light" %>
  </div>
</div>