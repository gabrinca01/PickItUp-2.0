<div class="challengeField">
  <%= link_to  challenge.description, challenge, class:"genText", style:"align:middle"%>
  <% if Pundit.policy(Current.user,challenge).destroy? %>
  <%= button_to "Delete",
                  challenges_path(challenge),
                  method: :delete,
                  class: "btn btn--light" %>
  <% end %>
</div>